
type User {
  id: ID
  name: String
  email: String
  password: String
  createdAt: String
  posts: [Post]
}

type Post {
  id: ID
  title: String
  body: String
  createdAt: String
}

type Query {
  getUsers: [User]
  getUser(id: ID): User
}
